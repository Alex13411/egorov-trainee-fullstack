# Production: set to your Render/Railway backend URL (no trailing slash).
# Local dev can omit this — auth.ts uses http://localhost:8000 in dev mode.
VITE_AUTH_API_BASE=https://your-backend.onrender.com
